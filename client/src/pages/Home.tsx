import { useEffect, useMemo, useState } from "react";
import { useAuth } from "@/_core/hooks/useAuth";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar";
import { BarChart3,   Bell, Download, Check, ChevronRight, Code2, ExternalLink, Github, Globe2, LayoutTemplate, LogOut, Palette, Pencil, Pin, Plus, Search, Settings2, Sparkles, Star, Terminal, TrendingUp, Users, Wand2, X } from "lucide-react";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";
import { toast } from "sonner";

const repos = [
  { name: "orbit-ui", description: "A small, intentional component system for expressive product interfaces.", language: "TypeScript", stars: 184, forks: 19, summary: "A thoughtful UI foundation that balances accessible primitives with a strong visual point of view.", color: "#3178c6", pinned: true },
  { name: "signal-cache", description: "Fast, typed caching for edge-first applications.", language: "Rust", stars: 92, forks: 8, summary: "A compact caching layer designed for predictable performance and composable invalidation.", color: "#dea584", pinned: true },
  { name: "notes-to-shipped", description: "Turn rough ideas into focused implementation plans.", language: "JavaScript", stars: 67, forks: 12, summary: "A practical workflow for moving from messy notes to small, shippable milestones.", color: "#f1e05a", pinned: false },
  { name: "tiny-cron", description: "A zero-dependency scheduler for small Node services.", language: "TypeScript", stars: 41, forks: 4, summary: "A minimal scheduling primitive that keeps operational complexity low for small services.", color: "#3178c6", pinned: false },
];

const templates = ["Atelier", "Terminal", "Editorial"];

export function PortfolioPreview({ compact = false }: { compact?: boolean }) {
  const [language, setLanguage] = useState("All");
  const slug = typeof window !== "undefined" ? window.location.pathname.split("/").filter(Boolean)[0] || "alexmorgan" : "alexmorgan";
  const { data: profile } = trpc.portfolio.bySlug.useQuery({ slug }, { enabled: !compact });
  const analyticsRecord = trpc.analytics.record.useMutation();
  useEffect(() => { if (compact) return; const params = new URLSearchParams(window.location.search); const referrer = params.get("utm_source") || document.referrer || undefined; const visitorHash = window.crypto?.randomUUID?.() || `${Date.now()}`; const consent = localStorage.getItem("githubfolio.analytics-consent") !== "denied"; void analyticsRecord.mutate({ slug, referrer, visitorHash, consent, utmSource: params.get("utm_source") || undefined, utmMedium: params.get("utm_medium") || undefined, utmCampaign: params.get("utm_campaign") || undefined }); }, [slug, compact]);
  const sourceRepos = profile?.repositories?.map(repo => ({ name: repo.name, description: repo.description || "A considered project from this developer’s public work.", language: repo.language || "Code", stars: repo.stars || 0, forks: repo.forks || 0, pinned: Boolean(repo.isPinned), color: "#5ca18c" })) ?? repos;
  const [languagesOverride] = useState<string[] | null>(null);
  const languages = languagesOverride ?? ["All", ...Array.from(new Set(sourceRepos.map(repo => repo.language)))];
  const visibleRepos = sourceRepos.filter(repo => language === "All" || repo.language === language);
  return <div className={`portfolio-preview-shell ${compact ? "rounded-3xl bg-[#11201d] p-5 text-white" : "min-h-screen bg-[#f8faf7] text-[#14221f]"}`}>
    <div className={compact ? "" : "mx-auto max-w-6xl px-6 py-8 lg:px-10"}>
      <div className="flex items-center justify-between border-b border-white/10 pb-5"><div className="flex items-center gap-3"><Avatar className="h-9 w-9"><AvatarImage alt="Alex Morgan avatar" src="https://avatars.githubusercontent.com/u/583231?v=4" /><AvatarFallback>AM</AvatarFallback></Avatar><span className="font-semibold">alexmorgan</span></div><div className="hidden items-center gap-5 text-xs text-white/55 sm:flex"><span>Work</span><span>Writing</span><span>About</span><span className="rounded-full border border-white/15 px-3 py-1.5">GitHub ↗</span></div></div>
      <section className={compact ? "py-12" : "grid gap-12 py-20 lg:grid-cols-[1.1fr_.9fr] lg:items-end"}><div><div className="mb-5 inline-flex items-center gap-2 rounded-full border border-[#cde7dc] bg-white px-3 py-1.5 text-xs font-medium text-[#287365]"><span className="h-1.5 w-1.5 rounded-full bg-[#5ac28b]" /> Available for thoughtful collaborations</div><h1 className="max-w-3xl text-4xl font-extrabold tracking-[-.055em] sm:text-6xl">Building calm, capable software for the messy middle.</h1><p className="mt-6 max-w-xl text-base leading-7 text-[#667870]">Product-minded engineer focused on interfaces, developer tools, and systems that help good ideas become useful things.</p><div className="mt-8 flex flex-wrap gap-3"><Button className="rounded-full bg-[#14221f] px-5 text-white hover:bg-[#28534b]">Explore my work <ChevronRight className="ml-1 h-4 w-4" /></Button><Button variant="outline" onClick={() => window.print()} className="rounded-full border-[#cfe0d8] bg-white">Print resume</Button></div></div>{!compact && <div className="rounded-[2rem] bg-[#123a32] p-7 text-white shadow-[0_24px_60px_rgba(20,55,45,.15)]"><div className="flex items-center justify-between text-xs text-white/50"><span className="font-mono">/now</span><span>2026</span></div><p className="mt-14 text-2xl font-semibold leading-tight">Making the invisible parts of software feel considered.</p><div className="mt-9 flex gap-2 text-xs text-white/65"><span className="rounded-full bg-white/10 px-3 py-1.5">TypeScript</span><span className="rounded-full bg-white/10 px-3 py-1.5">Product craft</span></div></div>}</section>
      <section className={compact ? "pt-2" : "border-t border-[#dce5df] py-14"}><div className="mb-7 flex items-end justify-between"><div><p className="font-mono text-xs uppercase tracking-[.18em] text-[#62a994]">Selected work</p><h2 className="mt-2 text-2xl font-bold tracking-[-.035em]">Things I’ve made</h2></div><span className="text-sm text-[#7b8b84]">04 projects</span></div><div className="mb-4 flex flex-wrap gap-2">{languages.map(item => <button key={item} onClick={() => setLanguage(item)} className={`rounded-full border px-3 py-1.5 text-xs font-medium ${language === item ? "border-[#123a32] bg-[#123a32] text-white" : "border-[#dce5df] bg-white text-[#81918b]"}`}>{item}</button>)}</div><div className="grid gap-4">{visibleRepos.slice(0, compact ? 2 : 4).map(repo => <div key={repo.name} className="group rounded-2xl border border-[#dce5df] bg-white p-5 transition hover:-translate-y-0.5 hover:shadow-lg"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2"><Code2 className="h-4 w-4 text-[#4c9582]" /><h3 className="font-bold">{repo.name}</h3>{repo.pinned && <Pin className="h-3.5 w-3.5 fill-[#4c9582] text-[#4c9582]" />}</div><p className="mt-2 max-w-xl text-sm leading-6 text-[#6b7d76]">{repo.description}</p></div><ExternalLink className="h-4 w-4 text-[#98aaa3]" /></div><div className="mt-5 flex flex-wrap items-center gap-4 text-xs text-[#81918b]"><span className="flex items-center gap-1.5"><i className="h-2 w-2 rounded-full" style={{ background: repo.color }} />{repo.language}</span><span><Star className="mr-1 inline h-3.5 w-3.5" />{repo.stars}</span><span>{repo.forks} forks</span><span className="ml-auto text-[#4b9280]">AI summary →</span></div></div>)}</div></section>
      {!compact && <><section className="grid gap-6 border-t border-[#dce5df] py-14 lg:grid-cols-2"><div><p className="font-mono text-xs uppercase tracking-[.18em] text-[#62a994]">Built in public</p><h2 className="mt-2 text-2xl font-bold">A little signal over time.</h2><div className="mt-5 grid grid-cols-12 gap-1.5 rounded-2xl border border-[#dce5df] bg-white p-5">{Array.from({length:72},(_,i)=><span key={i} className={`aspect-square rounded-[3px] ${i%11===0?"bg-[#123a32]":i%5===0?"bg-[#75bca4]":i%3===0?"bg-[#b9e5d1]":"bg-[#edf3ee]"}`} />)}</div></div><div><p className="font-mono text-xs uppercase tracking-[.18em] text-[#62a994]">Now</p><div className="mt-5 space-y-4"><p className="text-sm font-semibold">Exploring local-first software</p><p className="text-sm font-semibold">Latest note: Designing for the quiet moments</p></div></div></section><section className="border-t border-[#dce5df] py-12"><div className="flex flex-col justify-between gap-5 rounded-3xl bg-[#123a32] p-7 text-white sm:flex-row sm:items-center"><div><p className="font-mono text-xs uppercase tracking-[.18em] text-[#a8d9c3]">Stay in the loop</p><h2 className="mt-2 text-2xl font-bold">Occasional notes on making things.</h2></div><div className="flex max-w-sm gap-2"><input placeholder="you@example.com" className="min-w-0 flex-1 rounded-full bg-white/10 px-4 py-2.5 text-sm text-white outline-none placeholder:text-white/40" /><button className="rounded-full bg-[#d9f99d] px-4 py-2.5 text-xs font-bold text-[#234011]">Subscribe</button></div></div></section></>}
    </div>
  </div>;
}

function Sidebar({ active, setActive }: { active: string; setActive: (s: string) => void }) {
  const items = [["Overview", Sparkles], ["Portfolio editor", Pencil], ["Analytics", BarChart3], ["Templates", LayoutTemplate], ["Settings", Settings2]] as const;
  return <aside className="hidden w-64 shrink-0 border-r border-[#dce5df] bg-[#fbfcfa] p-5 lg:block"><div className="flex items-center gap-2.5 px-3 py-3"><div className="grid h-8 w-8 place-items-center rounded-xl bg-[#123a32] text-[#d9f99d]"><Terminal className="h-4 w-4" /></div><span className="font-bold tracking-[-.03em]">GitHubFolio</span></div><div className="mt-10 space-y-1">{items.map(([label, Icon]) => <button key={label} onClick={() => setActive(label)} className={`flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left text-sm font-medium ${active === label ? "bg-[#e3f0e8] text-[#123a32]" : "text-[#788981] hover:bg-[#f0f4f0] hover:text-[#123a32]"}`}><Icon className="h-4 w-4" />{label}</button>)}</div><div className="mt-auto pt-36"><div className="rounded-2xl bg-[#edf7d8] p-4"><p className="text-xs font-bold text-[#365b1e]">Make it yours</p><p className="mt-1 text-xs leading-5 text-[#67814a]">Upgrade for custom domains and advanced analytics.</p><button className="mt-3 text-xs font-bold text-[#365b1e]" onClick={() => setActive("Settings")}>View plans →</button></div><button className="mt-6 flex items-center gap-3 px-3 text-sm text-[#81918b]"><LogOut className="h-4 w-4" /> Sign out</button></div></aside>
}

function Dashboard() {
  const [active, setActive] = useState("Overview");
  const { data: integrationConfig } = trpc.integrations.useQuery();
  const [template, setTemplate] = useState("Atelier");
  const [showInstall, setShowInstall] = useState(true);
  const [css, setCss] = useState(".portfolio-card { border-radius: 24px; }\n.hero-title { letter-spacing: -0.05em; }");
  const content = useMemo(() => {
    if (active === "Portfolio editor") return <Editor />;
    if (active === "Analytics") return <Analytics />;
    if (active === "Templates") return <Templates template={template} setTemplate={setTemplate} />;
    if (active === "Settings") return <Settings billingEnabled={Boolean(integrationConfig?.paddleConfigured)} />;
    return <Overview setActive={setActive} />;
  }, [active, template]);
  return <div className="flex min-h-screen bg-[#f5f6f2] text-[#14221f]"><Sidebar active={active} setActive={setActive} /><main className="min-w-0 flex-1"><header className="flex h-20 items-center justify-between border-b border-[#dce5df] bg-[#fbfcfa]/80 px-5 backdrop-blur lg:px-10"><div><p className="font-mono text-[10px] uppercase tracking-[.2em] text-[#91a29a]">Workspace / {active}</p><h1 className="mt-1 text-lg font-bold tracking-[-.03em]">Good morning, Alex</h1></div><div className="flex items-center gap-3"><button aria-label="Open notifications" className="relative rounded-full p-2 text-[#7f9289] hover:bg-[#e9efea]"><Bell className="h-4 w-4" /><span className="absolute right-1.5 top-1.5 h-1.5 w-1.5 rounded-full bg-[#e67b54]" /></button><Avatar className="h-9 w-9 border-2 border-white shadow-sm"><AvatarImage alt="Alex Morgan avatar" src="https://avatars.githubusercontent.com/u/583231?v=4" /><AvatarFallback>AM</AvatarFallback></Avatar></div></header><div className="p-5 lg:p-10">{content}</div></main>{showInstall && <div className="fixed bottom-5 right-5 z-20 w-[min(360px,calc(100vw-40px))] rounded-2xl border border-[#cde2d7] bg-[#123a32] p-4 text-white shadow-2xl"><button aria-label="Dismiss install prompt" onClick={() => setShowInstall(false)} className="absolute right-3 top-3 text-white/50"><X className="h-4 w-4" /></button><div className="flex gap-3"><div className="grid h-9 w-9 shrink-0 place-items-center rounded-xl bg-[#d9f99d] text-[#234011]"><Bell className="h-4 w-4" /></div><div><p className="text-sm font-bold">Install GitHubFolio</p><p className="mt-1 text-xs leading-5 text-white/65">Add it to your home screen for quick access and portfolio updates.</p><button onClick={() => { void installPwa(); void requestNotifications(); }} className="mt-3 rounded-full bg-white px-3 py-1.5 text-xs font-bold text-[#123a32]">Install web app</button></div></div></div>}</div>
}

function Overview({ setActive }: { setActive: (s: string) => void }) { return <><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="font-mono text-xs uppercase tracking-[.2em] text-[#4c9582]">Your portfolio</p><h2 className="mt-2 text-4xl font-extrabold tracking-[-.06em]">A clearer way to show your work.</h2><p className="mt-3 max-w-xl text-sm leading-6 text-[#6f8179]">Your page is live and looking good. Keep shaping it into the version of your work you want people to remember.</p></div><Button onClick={() => setActive("Portfolio editor")} className="rounded-full bg-[#123a32] px-5 text-white hover:bg-[#28534b]"><Pencil className="mr-2 h-4 w-4" /> Edit portfolio</Button></div><div className="mt-9 grid gap-4 sm:grid-cols-3"><Metric label="Portfolio views" value="1,248" delta="+18.4%" icon={TrendingUp} /><Metric label="Unique visitors" value="836" delta="+12.2%" icon={Users} /><Metric label="Public projects" value="04" delta="2 pinned" icon={Code2} /></div><div className="mt-8 grid gap-6 xl:grid-cols-[1.25fr_.75fr]"><Card className="overflow-hidden rounded-3xl border-[#dce5df] bg-white p-6"><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.18em] text-[#91a29a]">Live preview</p><h3 className="mt-1 text-lg font-bold">alexmorgan.githubfolio.com</h3></div><a href="#" className="rounded-full border border-[#dce5df] p-2 text-[#799089]"><ExternalLink className="h-4 w-4" /></a></div><div className="mt-5 overflow-hidden rounded-2xl"><PortfolioPreview compact /></div></Card><div className="space-y-6"><Card className="rounded-3xl border-[#dce5df] bg-white p-6"><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.18em] text-[#91a29a]">Traffic</p><h3 className="mt-1 text-lg font-bold">Last 30 days</h3></div><BarChart3 className="h-5 w-5 text-[#4c9582]" /></div><div className="mt-8 flex h-28 items-end gap-1.5">{[32,45,38,58,47,64,52,76,59,71,68,86,73,91,78,98,87,84,100,93,76,82,88,94,80,89,96,100,92,100].map((h,i) => <div key={i} className="flex-1 rounded-t-md bg-[#b9e5d1]" style={{height:`${h}%`}} />)}</div><div className="mt-3 flex justify-between text-[10px] text-[#9aa9a2]"><span>Jun 18</span><span>Jun 25</span><span>Jul 02</span></div></Card><Card className="rounded-3xl border-[#dce5df] bg-[#edf7d8] p-6"><div className="flex items-center gap-2 text-[#52752e]"><Sparkles className="h-4 w-4" /><span className="text-xs font-bold">AI showcase</span></div><p className="mt-3 text-sm leading-6 text-[#4c6335]">Your project summaries are helping visitors understand the thinking behind your work.</p><button onClick={() => setActive("Portfolio editor")} className="mt-4 text-xs font-bold text-[#365b1e]">Review summaries →</button></Card></div></div></> }
function Metric({ label, value, delta, icon: Icon }: any) { return <Card className="rounded-2xl border-[#dce5df] bg-white p-5"><div className="flex items-center justify-between"><span className="text-xs text-[#7f9289]">{label}</span><Icon className="h-4 w-4 text-[#5ca18c]" /></div><div className="mt-5 flex items-end gap-2"><span className="text-3xl font-extrabold tracking-[-.06em]">{value}</span><span className="mb-1 text-[11px] font-bold text-[#5ca18c]">{delta}</span></div></Card> }
function Editor() {
  const [items,setItems]=useState(repos); const [query,setQuery]=useState(""); const [selected,setSelected]=useState<string[]>([]); const [showSections,setShowSections]=useState(true); const [bio,setBio]=useState("Product-minded engineer focused on interfaces, developer tools, and systems.");
  const filtered=items.filter(x=>x.name.toLowerCase().includes(query.toLowerCase()) || x.language.toLowerCase().includes(query.toLowerCase()));
  const toggle=(name:string)=>setSelected(current=>current.includes(name)?current.filter(x=>x!==name):[...current,name]);
  return <><PageTitle eyebrow="Portfolio editor" title="Shape the story." subtitle="Choose what gets attention, then let the details do the talking." action={<div className="flex gap-2"><Button variant="outline" className="rounded-full bg-white" onClick={()=>toast.success("Draft saved")}>Save draft</Button><Button className="rounded-full bg-[#123a32] text-white" onClick={()=>toast.success("Portfolio published")}><Check className="mr-2 h-4 w-4" /> Publish</Button></div>} /><div className="mt-8 grid gap-6 xl:grid-cols-[1fr_360px]"><Card className="rounded-3xl border-[#dce5df] bg-white p-6"><div className="mb-5 flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div><h3 className="font-bold">Projects in your portfolio</h3><p className="mt-1 text-xs text-[#899a92]">Drag to reorder, pin the strongest work, and hide anything unfinished.</p></div><Badge className="bg-[#edf7d8] text-[#52752e]">{items.length} synced</Badge></div><div className="mb-4 flex gap-2"><Input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search repositories or languages" className="rounded-xl bg-[#f5f7f4]"/><Button variant="outline" className="rounded-xl bg-white" onClick={()=>{setItems(items.filter(x=>!selected.includes(x.name)));setSelected([])}} disabled={!selected.length}>Hide selected</Button></div><div className="space-y-3">{filtered.map((r,i)=><div key={r.name} className="flex items-center gap-4 rounded-2xl border border-[#e4ebe6] p-4"><input aria-label={`Select ${r.name}`} type="checkbox" checked={selected.includes(r.name)} onChange={()=>toggle(r.name)} className="h-4 w-4 accent-[#123a32]"/><div className="cursor-grab text-[#a1b0a9]">⠿</div><div className="grid h-9 w-9 place-items-center rounded-xl bg-[#eaf4ef] text-[#4c9582]"><Code2 className="h-4 w-4" /></div><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><p className="font-bold">{r.name}</p>{r.pinned && <Pin className="h-3 w-3 fill-[#4c9582] text-[#4c9582]" />}</div><p className="truncate text-xs text-[#899a92]">{r.description}</p></div><button aria-label={`Toggle pin for ${r.name}`} onClick={()=>setItems(items.map(x=>x.name===r.name?{...x,pinned:!x.pinned}:x))} className={`rounded-full p-2 ${r.pinned?"bg-[#e3f0e8] text-[#438b75]":"text-[#a0aea8] hover:bg-[#f0f4f0]"}`}><Pin className="h-4 w-4" /></button></div>)}</div><div className="mt-5 flex flex-wrap gap-2"><Button variant="outline" className="rounded-full bg-white text-xs" onClick={()=>toast.success("Manual project draft added")}><Plus className="mr-2 h-4 w-4"/>Add manual project</Button><Button variant="outline" className="rounded-full bg-white text-xs" onClick={()=>toast.success("Repository exclusions saved")}>Manage exclusions</Button></div></Card><div className="space-y-6"><Card className="rounded-3xl border-[#dce5df] bg-white p-6"><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.18em] text-[#91a29a]">Story copy</p><h3 className="mt-1 font-bold">About this portfolio</h3></div><Wand2 className="h-4 w-4 text-[#4c9582]" /></div><Textarea value={bio} onChange={e=>setBio(e.target.value)} className="mt-4 min-h-28 rounded-xl bg-[#f5f7f4] text-sm"/><Button variant="outline" className="mt-3 rounded-full bg-white text-xs" onClick={()=>toast.success("AI headline suggestion ready")}>Suggest headline</Button></Card><Card className="rounded-3xl border-[#dce5df] bg-white p-6"><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.18em] text-[#91a29a]">AI showcase</p><h3 className="mt-1 font-bold">Tune the narrative</h3></div><Sparkles className="h-4 w-4 text-[#4c9582]" /></div><div className="mt-4 grid gap-3"><label className="text-xs font-medium text-[#6f8179]">Tone<select className="mt-1 w-full rounded-xl border border-[#dce5df] bg-[#f5f7f4] px-3 py-2 text-xs"><option>Thoughtful</option><option>Technical</option><option>Playful</option></select></label><label className="text-xs font-medium text-[#6f8179]">Summary length<select className="mt-1 w-full rounded-xl border border-[#dce5df] bg-[#f5f7f4] px-3 py-2 text-xs"><option>Short</option><option>Standard</option><option>Detailed</option></select></label></div><div className="mt-4 flex flex-wrap gap-2"><Button onClick={()=>toast.success("Repository summaries regenerated")} className="rounded-full bg-[#123a32] text-xs text-white">Regenerate summaries</Button><Button variant="outline" onClick={()=>toast.success("Technology clusters refreshed")} className="rounded-full bg-white text-xs">Cluster skills</Button></div></Card><Card className="rounded-3xl border-[#dce5df] bg-white p-6"><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.18em] text-[#91a29a]">Sections</p><p className="mt-1 text-xs text-[#899a92]">Toggle and reorder the public story.</p></div><button aria-label="Toggle section editor" onClick={()=>setShowSections(!showSections)}><Settings2 className="h-4 w-4 text-[#4c9582]" /></button></div>{showSections && <div className="mt-4 space-y-3">{["Hero introduction","Selected work","Writing","Currently learning","Contact link"].map((x,i)=><div key={x} className="flex items-center justify-between rounded-xl border border-[#e4ebe6] p-3"><span className="text-sm font-medium">{x}</span><div className="flex items-center gap-3"><span className="cursor-grab text-[#a1b0a9]">⠿</span><div className={`h-5 w-9 rounded-full p-0.5 ${i===3?"bg-[#cbd8d0]":"bg-[#123a32]"}`}><div className={`h-4 w-4 rounded-full bg-white shadow-sm ${i===3?"":"ml-4"}`} /></div></div></div>)}</div>}</Card></div></div></>
}
function Analytics() {
  const [range, setRange] = useState("30 days");
  const [consent, setConsent] = useState(() => { try { return localStorage.getItem("githubfolio.analytics-consent") !== "denied"; } catch { return true; } });
  const [digest, setDigest] = useState(true);
  const rangeCount = range === "7 days" ? 7 : range === "90 days" ? 90 : 30;
  const { data: summary } = trpc.analytics.summary.useQuery({ days: rangeCount });
  const chartData = (summary?.series ?? (summary?.timeseries ?? [32,45,38,58,47,64,52,76,59,71,68,86,73,91,78]).map((views, index) => ({ date: `day-${index + 1}`, views }))).slice(-rangeCount);
  const exportCsv = () => {
    const rows = ["date,views,campaign", ...chartData.map((point) => `${point.date},${point.views},${summary?.referrals?.[0]?.name || "direct"}`)];
    const url = URL.createObjectURL(new Blob([rows.join("\\n")], { type: "text/csv" }));
    const anchor = document.createElement("a"); anchor.href = url; anchor.download = "githubfolio-analytics.csv"; anchor.click(); URL.revokeObjectURL(url); toast.success("Analytics CSV exported");
  };
  return <>
    <PageTitle eyebrow="Analytics" title="See what resonates." subtitle="A calm view of how people discover your work." action={<div className="flex gap-2">{["7 days", "30 days", "90 days"].map((option) => <button key={option} onClick={() => setRange(option)} className={`rounded-full px-3 py-2 text-xs font-bold ${range === option ? "bg-[#123a32] text-white" : "bg-white text-[#81918b]"}`}>{option}</button>)}<Button variant="outline" onClick={exportCsv} className="rounded-full bg-white"><Download className="mr-2 h-4 w-4" /> CSV</Button></div>} />
    <div className="mt-8 grid gap-4 sm:grid-cols-3"><Metric label="Total views" value={String(summary?.totalViews ?? 1248)} delta="+18.4%" icon={TrendingUp} /><Metric label="Unique visitors" value={String(summary?.uniqueVisitors ?? 836)} delta="+12.2%" icon={Users} /><Metric label="Avg. session" value="02:48" delta="+08.1%" icon={BarChart3} /></div>
    <div className="mt-6 grid gap-6 lg:grid-cols-[1.35fr_.65fr]"><Card className="rounded-3xl border-[#dce5df] bg-white p-6"><div className="flex items-center justify-between"><h3 className="font-bold">Traffic over time</h3><span className="text-xs text-[#899a92]">{range}</span></div><div className="mt-8 flex h-64 items-end gap-2">{chartData.map((point, index) => <div key={`${point.date}-${index}`} className="flex-1 rounded-t-lg bg-gradient-to-t from-[#b5e3cf] to-[#5ca18c]" style={{ height: `${Math.max(4, point.views)}%` }} />)}</div><div className="mt-4 flex justify-between text-xs text-[#94a39c]"><span>{chartData[0]?.date || "Start"}</span><span>{chartData[Math.floor(chartData.length / 2)]?.date || "Mid"}</span><span>{chartData.at(-1)?.date || "Today"}</span></div></Card><Card className="rounded-3xl border-[#dce5df] bg-white p-6"><h3 className="font-bold">Top locations</h3><div className="mt-6 space-y-5">{(summary?.countries ?? []).map((country) => <div key={country.name}><div className="flex justify-between text-sm"><span className="font-medium">{country.name}</span><span className="text-[#83928b]">{country.value}</span></div><div className="mt-2 h-1.5 rounded-full bg-[#edf1ed]"><div className="h-full rounded-full bg-[#5ca18c]" style={{ width: `${Math.min(100, country.value)}` }} /></div><p className="mt-1 text-xs text-[#9aa9a2]">{summary?.regions?.find((region) => region.name !== "Unknown")?.name || "Unknown region"}</p></div>)}</div></Card></div>
    <div className="mt-6 grid gap-6 lg:grid-cols-2"><Card className="rounded-3xl border-[#dce5df] bg-white p-6"><div className="flex items-center justify-between"><div><h3 className="font-bold">Privacy controls</h3><p className="mt-1 text-xs text-[#899a92]">Respect visitor consent before recording analytics.</p></div><button aria-label="Toggle analytics consent" onClick={() => { const next = !consent; setConsent(next); localStorage.setItem("githubfolio.analytics-consent", next ? "granted" : "denied"); }} className={`h-6 w-11 rounded-full p-0.5 ${consent ? "bg-[#123a32]" : "bg-[#cbd8d0]"}`}><span className={`block h-5 w-5 rounded-full bg-white transition ${consent ? "ml-5" : ""}`} /></button></div><p className="mt-5 text-xs leading-5 text-[#899a92]">{consent ? "Consent-aware analytics is enabled." : "Analytics collection is disabled until visitors opt in."}</p></Card><Card className="rounded-3xl border-[#dce5df] bg-white p-6"><div className="flex items-center justify-between"><div><h3 className="font-bold">Portfolio update digest</h3><p className="mt-1 text-xs text-[#899a92]">Receive a weekly summary of traffic and referrals.</p></div><button aria-label="Toggle portfolio digest" onClick={() => setDigest(!digest)} className={`h-6 w-11 rounded-full p-0.5 ${digest ? "bg-[#123a32]" : "bg-[#cbd8d0]"}`}><span className={`block h-5 w-5 rounded-full bg-white transition ${digest ? "ml-5" : ""}`} /></button></div><div className="mt-5 rounded-2xl bg-[#f5f7f4] p-4"><p className="text-xs font-mono uppercase tracking-[.16em] text-[#91a29a]">Campaign tracking</p><p className="mt-2 text-sm font-semibold">{summary?.referrals?.[0]?.name || "direct"} · {summary?.referrals?.[0]?.value || 0}</p><p className="mt-1 text-xs text-[#899a92]">Source, medium, and campaign attribution from recent visits</p></div></Card></div>
  </>;
}
function Templates({template,setTemplate}:{template:string;setTemplate:(s:string)=>void}) {
  const [density,setDensity]=useState("Comfortable");
  const [accent,setAccent]=useState("Pine");
  const [device,setDevice]=useState("Desktop");
  const [css,setCss]=useState(".portfolio-card { border-radius: 24px; }\\n.hero-title { letter-spacing: -0.05em; }");
  useEffect(() => { localStorage.setItem("githubfolio-template", template); localStorage.setItem("githubfolio-accent", accent); localStorage.setItem("githubfolio-density", density); localStorage.setItem("githubfolio-css", css); }, [template, accent, density, css]);
  const accentColor = accent === "Plum" ? "#45365e" : accent === "Cobalt" ? "#244f7a" : accent === "Ember" ? "#8c4b36" : "#123a32";
  const densitySpace = density === "Compact" ? "1.25rem" : density === "Airy" ? "4rem" : "2.5rem";
  return <><style>{`.portfolio-preview-shell h1 { letter-spacing: ${density === "Compact" ? "-.035em" : "-.055em"}; } .portfolio-preview-shell section { padding-top: ${densitySpace}; padding-bottom: ${densitySpace}; } .portfolio-preview-shell .accent-signal { color: ${accentColor}; } ${css}`}</style><PageTitle eyebrow="Templates" title="Pick your point of view." subtitle="Every template is designed to let the work lead." action={<Button variant="outline" className="rounded-full bg-white" onClick={()=>toast.success("A new revision was saved")}>Save revision</Button>} /><div className="mt-8 grid gap-6 xl:grid-cols-[1fr_360px]"><div><div className="mb-4 flex items-center justify-between"><div className="flex gap-2">{["Desktop","Tablet","Mobile"].map(x=><button key={x} onClick={()=>setDevice(x)} className={`rounded-full px-3 py-1.5 text-xs font-bold ${device===x?"bg-[#123a32] text-white":"bg-white text-[#81918b]"}`}>{x}</button>)}</div><button onClick={()=>toast.success("Template duplicated as a private draft")} className="text-xs font-bold text-[#4c9582]">Duplicate template ↗</button></div><div className={`mx-auto overflow-hidden rounded-3xl transition-all ${device==="Mobile"?"max-w-sm":device==="Tablet"?"max-w-2xl":"max-w-none"}`}><PortfolioPreview compact /></div></div><div className="space-y-5"><Card className="rounded-3xl border-[#dce5df] bg-white p-6"><p className="font-mono text-[10px] uppercase tracking-[.18em] text-[#91a29a]">Layouts</p><div className="mt-4 space-y-2">{templates.map((t,i)=><button key={t} onClick={()=>{setTemplate(t);toast.success(`${t} template selected`)}} className={`flex w-full items-center justify-between rounded-2xl border p-3 text-left ${template===t?"border-[#5ca18c] bg-[#edf7d8]":"border-[#e4ebe6]"}`}><span><span className="block text-sm font-bold">{t}</span><span className="text-xs text-[#899a92]">{i===0?"Warm editorial":i===1?"Dark technical":"Quiet minimal"}</span></span>{template===t&&<Check className="h-4 w-4 text-[#4c9582]"/>}</button>)}</div></Card><Card className="rounded-3xl border-[#dce5df] bg-white p-6"><p className="font-mono text-[10px] uppercase tracking-[.18em] text-[#91a29a]">Visual system</p><div className="mt-4"><p className="text-sm font-bold">Accent palette</p><div className="mt-3 flex gap-2">{[["Pine","#123a32"],["Plum","#45365e"],["Cobalt","#244f7a"],["Ember","#8c4b36"]].map(([name,color])=><button key={name} title={name} onClick={()=>setAccent(name)} className={`h-7 w-7 rounded-full border-2 ${accent===name?"border-[#14221f] ring-2 ring-[#d9f99d]":"border-white"}`} style={{backgroundColor:color}} />)}</div></div><div className="mt-5"><p className="text-sm font-bold">Layout density</p><div className="mt-3 flex gap-2">{["Compact","Comfortable","Airy"].map(x=><button key={x} onClick={()=>setDensity(x)} className={`rounded-full border px-3 py-1.5 text-xs ${density===x?"border-[#5ca18c] bg-[#edf7d8] text-[#365b1e]":"border-[#dce5df] text-[#81918b]"}`}>{x}</button>)}</div></div><div className="mt-5"><p className="text-sm font-bold">Custom CSS</p><Textarea className="mt-3 min-h-28 rounded-xl bg-[#14221f] font-mono text-xs text-[#d9f99d]" value={css} onChange={(event)=>setCss(event.target.value)}/></div></Card><Card className="rounded-3xl border-[#dce5df] bg-white p-6"><div className="flex items-center justify-between"><p className="font-mono text-[10px] uppercase tracking-[.18em] text-[#91a29a]">Revision history</p><HistoryIcon /></div><div className="mt-4 space-y-3">{["Current draft · just now","Atelier · 2 hours ago","First publish · yesterday"].map((x,i)=><div key={x} className="flex items-center justify-between text-xs"><span className={i===0?"font-bold":"text-[#899a92]"}>{x}</span>{i>0&&<button className="font-bold text-[#4c9582]" onClick={()=>toast.info(`Restoring ${x}`)}>Restore</button>}</div>)}</div></Card></div></div></>
}
function HistoryIcon(){ return <span className="text-[#4c9582]">↺</span> }
function Settings({ billingEnabled = false }: { billingEnabled?: boolean }) { return <><PageTitle eyebrow="Settings" title="Your account, your rules." subtitle="Manage connections, your public address, and what your portfolio shares." /><div className="mt-8 grid gap-6 lg:grid-cols-2"><Card className="rounded-3xl border-[#dce5df] bg-white p-6"><div className="flex items-center gap-3"><Github className="h-5 w-5"/><div><h3 className="font-bold">GitHub connection</h3><p className="text-xs text-[#83928b]">Sync public profile and repositories.</p></div><Badge className="ml-auto bg-[#d9f99d] text-[#365b1e]">Connected</Badge></div><div className="mt-6 flex items-center gap-3 rounded-2xl bg-[#f5f7f4] p-4"><Avatar><AvatarImage alt="Alex Morgan avatar" src="https://avatars.githubusercontent.com/u/583231?v=4"/><AvatarFallback>AM</AvatarFallback></Avatar><div className="flex-1"><p className="text-sm font-bold">alexmorgan</p><p className="text-xs text-[#83928b]">Last synced 8 minutes ago</p></div><Button variant="outline" className="rounded-full bg-white text-xs">Sync now</Button></div></Card><Card className="rounded-3xl border-[#dce5df] bg-white p-6"><div className="flex items-center gap-3"><Globe2 className="h-5 w-5 text-[#4c9582]"/><div><h3 className="font-bold">Public address</h3><p className="text-xs text-[#83928b]">Your default hosted portfolio URL.</p></div></div><div className="mt-6 flex gap-2"><Input defaultValue="alexmorgan" className="rounded-xl bg-[#f5f7f4]"/><Button className="rounded-xl bg-[#123a32] text-white">Save</Button></div><p className="mt-3 text-xs text-[#83928b]">githubfolio.com/<span className="font-semibold text-[#123a32]">alexmorgan</span></p></Card><Card className="rounded-3xl border-[#dce5df] bg-white p-6"><h3 className="font-bold">Custom domain</h3><p className="mt-1 text-xs text-[#83928b]">Connect a domain on the Pro plan.</p><div className="mt-5 flex gap-2"><Input placeholder="yourdomain.com" className="rounded-xl bg-[#f5f7f4]"/><Button variant="outline" className="rounded-xl bg-white">Connect</Button></div><p className="mt-3 flex items-center gap-1.5 text-xs text-[#d17a5d]"><span className="h-1.5 w-1.5 rounded-full bg-[#d17a5d]"/> Upgrade required</p></Card>{billingEnabled && <Card className="rounded-3xl border-[#dce5df] bg-[#123a32] p-6 text-white"><div className="flex items-center gap-3"><Sparkles className="h-5 w-5 text-[#d9f99d]"/><div><h3 className="font-bold">GitHubFolio Pro plans</h3><p className="text-xs text-white/55">More room for your work to travel further.</p></div></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><div className="rounded-2xl bg-white/10 p-4"><p className="text-xs font-bold text-[#d9f99d]">Pro</p><p className="mt-2 text-2xl font-extrabold">$6<span className="text-xs font-normal text-white/50"> / month</span></p><p className="mt-2 text-xs leading-5 text-white/65">Custom domain, 3 templates, advanced analytics, and 20 AI summaries per month.</p></div><div className="rounded-2xl border border-[#d9f99d]/40 bg-[#d9f99d]/10 p-4"><p className="text-xs font-bold text-[#d9f99d]">Pro+</p><p className="mt-2 text-2xl font-extrabold">$12<span className="text-xs font-normal text-white/50"> / month</span></p><p className="mt-2 text-xs leading-5 text-white/65">Unlimited templates and AI summaries, custom CSS, domain mapping, and deeper analytics.</p></div></div><Button onClick={()=>toast.info("Paddle checkout will open once billing credentials are configured.")} className="mt-6 w-full rounded-full bg-[#d9f99d] text-[#234011] hover:bg-[#c8ed83]">Choose a plan with Paddle</Button></Card>}<Card className="rounded-3xl border-[#dce5df] bg-white p-6"><div className="flex items-center justify-between"><div><h3 className="font-bold">Portfolio visibility</h3><p className="mt-1 text-xs text-[#83928b]">Control who can see your public page.</p></div><select className="rounded-xl border border-[#dce5df] bg-[#f5f7f4] px-3 py-2 text-xs"><option>Public</option><option>Private</option><option>Preview link only</option></select></div><div className="mt-5 rounded-2xl bg-[#f5f7f4] p-4"><p className="text-xs font-bold">Custom domain checklist</p><p className="mt-2 text-xs leading-5 text-[#83928b]">Add a CNAME record pointing to domains.githubfolio.com, then verify DNS here. SSL health will appear after propagation.</p><button onClick={()=>toast.info("DNS verification instructions copied")} className="mt-3 text-xs font-bold text-[#4c9582]">Copy DNS instructions ↗</button></div></Card><Card className="rounded-3xl border-[#dce5df] bg-white p-6"><h3 className="font-bold">Privacy and data</h3><p className="mt-1 text-xs text-[#83928b]">Manage browser notifications and your account data.</p><div className="mt-5 flex flex-wrap gap-2"><Button onClick={()=>void requestNotifications()} variant="outline" className="rounded-full bg-white text-xs">Enable notifications</Button><Button onClick={()=>toast.success("Export prepared")} variant="outline" className="rounded-full bg-white text-xs">Export account data</Button><Button onClick={()=>toast.warning("Deletion requires confirmation")} variant="outline" className="rounded-full bg-white text-xs text-[#b85f48]">Delete account</Button></div></Card></div></> }
async function installPwa() { const prompt = (window as Window & { deferredInstallPrompt?: Event }).deferredInstallPrompt as (Event & { prompt?: () => Promise<void> }) | undefined; if (prompt?.prompt) { await prompt.prompt(); delete (window as Window & { deferredInstallPrompt?: Event }).deferredInstallPrompt; } else { toast.info("Use your browser menu to install GitHubFolio"); } }
async function requestNotifications() { if (!("Notification" in window)) { toast.error("Browser notifications are not supported here."); return; } const permission = await Notification.requestPermission(); try { localStorage.setItem("githubfolio.notifications", permission); } catch { /* storage may be unavailable */ } toast[permission === "granted" ? "success" : "info"](permission === "granted" ? "Notifications enabled" : "Notifications remain off"); }
function PageTitle({eyebrow,title,subtitle,action}:{eyebrow:string;title:string;subtitle:string;action?:React.ReactNode}) { return <div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="font-mono text-xs uppercase tracking-[.2em] text-[#4c9582]">{eyebrow}</p><h2 className="mt-2 text-4xl font-extrabold tracking-[-.06em]">{title}</h2><p className="mt-3 text-sm text-[#6f8179]">{subtitle}</p></div>{action}</div> }

export default function Home() {
  const { isAuthenticated } = useAuth();
  const [mode, setMode] = useState<"landing" | "dashboard" | "portfolio">("landing");
  if (mode === "portfolio") return <><div className="fixed right-6 top-6 z-10"><Button onClick={() => setMode("landing")} className="rounded-full bg-[#123a32] text-white shadow-lg">Back to GitHubFolio</Button></div><PortfolioPreview /></>;
  if (mode === "dashboard" || isAuthenticated) return <Dashboard />;
  return <div className="min-h-screen overflow-hidden bg-[#f5f6f2]">
    <header className="mx-auto flex max-w-7xl items-center justify-between px-6 py-6 lg:px-10"><div className="flex items-center gap-2.5"><div className="grid h-9 w-9 place-items-center rounded-xl bg-[#123a32] text-[#d9f99d]"><Terminal className="h-4 w-4" /></div><span className="font-bold tracking-[-.03em]">GitHubFolio</span></div><div className="flex items-center gap-3"><button onClick={() => setMode("portfolio")} className="hidden text-sm font-medium text-[#6f8179] hover:text-[#123a32] sm:block">View example</button><Button onClick={() => startLogin()} variant="outline" className="rounded-full bg-white">Sign in</Button></div></header>
    <main className="mx-auto max-w-7xl px-6 pb-16 pt-10 lg:px-10 lg:pt-20"><div className="grid items-center gap-16 lg:grid-cols-[.9fr_1.1fr]"><div><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-[#cde7dc] bg-white px-3 py-1.5 text-xs font-semibold text-[#287365]"><Sparkles className="h-3.5 w-3.5" /> For developers with something to say</div><h1 className="max-w-xl text-5xl font-extrabold leading-[.98] tracking-[-.075em] sm:text-7xl">Your work deserves a <span className="text-[#4c9582]">better frame.</span></h1><p className="mt-7 max-w-lg text-base leading-7 text-[#6f8179]">Connect GitHub. Choose a point of view. Publish a portfolio that feels like you—not another grid of links.</p><div className="mt-8 flex flex-wrap gap-3"><Button onClick={() => startLogin()} className="rounded-full bg-[#123a32] px-6 text-white hover:bg-[#28534b]"><Github className="mr-2 h-4 w-4" /> Build my portfolio</Button><Button onClick={() => setMode("portfolio")} variant="outline" className="rounded-full bg-white px-6">See an example <ChevronRight className="ml-1 h-4 w-4" /></Button></div></div><div className="relative"><div className="absolute -inset-8 rounded-[4rem] bg-[#d9f99d]/30 blur-3xl" /><div className="relative rounded-[2rem] border border-[#dbe6df] bg-white p-3 shadow-[0_30px_100px_rgba(33,76,62,.14)]"><PortfolioPreview compact /></div></div></div></main>
  </div>;
}
